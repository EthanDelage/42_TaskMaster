process:
  echo:
    cmd: "echo test"
    stdout: test/out/cmd_echo.out
# This process will immediately exit
  invalid_cmd:
    cmd: "sleep test"
    stderr: test/out/cmd_invalid_cmd.err
  absolute_cmd:
    cmd: "/bin/ls"
    stdout: test/out/cmd_absolute_cmd.out
# This process won't start since the command cannot be found
#  unknown_cmd:
#    cmd: "unknown"

